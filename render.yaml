services:
  - type: web
    name: evm-voting-system
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s dist -p $PORT
    envVars:
      - key: NODE_VERSION
        value: 18
